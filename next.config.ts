// =============================================================================
// Next.js configuration.
//
// Loaded by Next at build/dev startup. The default export is merged with
// Next's internal defaults — anything we don't set falls back to those.
//
// This project is intentionally minimal: no custom webpack, no rewrites, no
// image domains, no experimental flags. If you need any of those, add them
// to the object below — see https://nextjs.org/docs/app/api-reference/config/next-config-js
// for the full list of options.
//
// Note: this file is `.ts` (not `.js`) so the NextConfig type below gives
// us editor autocomplete and type-checking for any options we add.
// =============================================================================
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
